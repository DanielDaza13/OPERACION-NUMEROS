html
    head
        meta(charset="utf-8")
        meta(name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no")
        link(rel="stylesheet" href="/static/css/bootstrap.min.css")
        link(rel="stylesheet" href="/static/css/styles.css")
        title="index.js"
    body
        div(class='bg')
            div(class="alert alert-danger d-none" role="alert" id="alert") Ingrese un numero valido
            div(class="d-flex flex-column justify-content-center align-items-center")
                h1 Hola #{name}
            div(class='d-flex flex-column justify-content-center align-items-center')
                div(class="form-group mt-5")
                    label(for="binary") Ingrese el numero binario
                    input(type="number" class="form-control"  id="binary")
                div(class="form-group mt-2")
                    button(class="btn btn-primary" onclick="convert()") Convertir
            div(id="result" class="d-none d-flex flex-column justify-content-center align-items-center")
                h1(id="numberConverted")
    script(src="/static/js/jquery.min.js")
    script(src="/static/js/bootstrap.min.js")
    script(src="/static/js/scripts.js")
        